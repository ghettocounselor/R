# we don't commonly in R that we'll use the While Loop
# it's useful though to understand this

# the bit in () will be checked if it's true, if so it'll execute the bits in {}
while(FALSE){
  print("Hello")
}

#counter loop
counter <- 1
while(counter < 12){
  print(counter)
  counter <- counter +1
}